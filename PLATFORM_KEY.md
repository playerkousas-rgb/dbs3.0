# 平台萬用密鑰（PLATFORM_KEY）— 後備通道

**定位：平台管理員的後備通道。無入口、無按鈕、無 UI；只在必要時用。**

前端完全沒有超管頁面、沒有連結、沒有痕跡。日常一律照舊：各區秘書／ADC 用自己區的密鑰運作。
平台管理員（Scout System）只需要記住**一組**平台密碼，就可以在任何一區登入。

---

## 一、要設定什麼（總共兩處）

### 1. Vercel — 只加一個環境變數

| 名稱 | 值 |
| --- | --- |
| `SUPER_KEY` | 平台萬用密鑰（建議 20 字以上） |

Settings → Environment Variables → 新增 → Redeploy。**各區的 STAFF_TOKEN / ADC_TOKEN 不需要放上來。**

### 2. 各區 Google Sheet — 設定一次

在該區的 Google Sheet 選單：

```text
🏕️ DBS 管理 → 🔐 設定平台萬用密鑰
```

貼上同一組 `SUPER_KEY` 即可。

- Config 只會儲存 **SHA-256 雜湊**（`SUPER_KEY_HASH`），唔會儲存明文，區方無法還原。
- 如要停用某區的後備通道：`🧹 清除平台萬用密鑰`。
- （可選）不想用雜湊，亦可自行在 Config 加一列 `SUPER_ACCOUNT`，值＝你想用的萬用帳戶名；但明文會俾區方看到，唔建議。

> 新區接入時：貼上最新 GS 模板並執行 `setupSystem()`，之後同樣在選單設定一次即可。

---

## 二、怎麼用

在**任何一區**的正常入口，把平台密鑰當作該區密鑰輸入：

| 想做什麼 | 去哪 | 輸入 |
| --- | --- | --- |
| ADC 主考審批 | 該區 `/adc` → ADC 審批後台 | ADC 密鑰 = 平台密鑰 |
| 該區說明文件 | 該區 `/adc` → 說明 | ADC 密鑰 = 平台密鑰 |
| 秘書後台（批核／證書／列印清單／主考名單） | 該區 `/admin` | STAFF_TOKEN = 平台密鑰 |

登入後權限等同該區最高權限；換區就換去另一區的入口，用同一組密鑰。

---

## 三、安全要點

| 項目 | 做法 |
| --- | --- |
| 密碼存哪 | 只在 Vercel 環境變數 `SUPER_KEY`，不會出現在前端 JS、Google Sheet 或 Git |
| 各區存什麼 | 只有 `SUPER_KEY_HASH` = SHA-256，無法還原明文 |
| 區方可否冒用 | 不可以：區方只能看到雜湊值，無法用它登入 |
| 想即刻全部失效 | 改 `SUPER_KEY`（或清除各區 `SUPER_KEY_HASH`）即可 |
| 唔想俾人知 | 前端零入口、零提示；GS 選單文字只用「平台萬用密鑰」字眼 |

> ⚠️ 換 `SUPER_KEY` 之後，記得在每一區重新執行一次「🔐 設定平台萬用密鑰」，否則後備通道會失效（安全設計，不是 bug）。

---

## 四、其他

- 平台密鑰只等於「該區最高權限」：它不會繞過 Apps Script 的業務邏輯（例如仍要符合批核流程、主考指派模式等），亦只會操作你登入那一個區的資料。
- 區接入時各區仍然只需提交自己的 `/exec URL` + API Key；Vercel 只需要該區的 `DBS_{區碼}_APIKEY`。
- 想檢查某區是否已設好後備通道：在該區 Sheet 睇 `Config → SUPER_KEY_HASH` 是否有值（只會見到雜湊）。

```text
© 2026 Scout System
```

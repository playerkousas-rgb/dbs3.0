# ─────────────────────────────────────────────────────────────
# DBS 3.0 多區版 — 環境變數範本
#
# 設定位置：Vercel → 你的 Project → Settings → Environment Variables
# 本檔案只作文件用途（.env.example），唔會被打包進前端 JS。
# 切勿把真實值 commit 上 Git。
# ─────────────────────────────────────────────────────────────

# ── 平台帳戶（後備通道）────────────────────────────────────
# 只加這一個就夠；各區的 STAFF_TOKEN / ADC_TOKEN 不需要放上來。
#   · 帳號名：寫在各區 Google Sheet 的 Config（SUPER_ACCOUNT，預設 sheep）
#   · 密碼  ：就是這個 SUPER_KEY，只在伺服器端比對，唔會傳去 Google
# 登入方式：在該區的 /admin 或 /adc，密鑰欄輸入「帳號:密碼」，例如 sheep:你的密碼
# 詳細說明見 PLATFORM_KEY.md
SUPER_KEY=

# ── 各區 API Key（每接入一區加一條）────────────────────────
# 由該區 setupSystem() 產生；前端只會經 /api/proxy 使用，不會外洩。
DBS_SKW_APIKEY=

# （新區範本）
# DBS_CHW_APIKEY=

# ─────────────────────────────────────────────────────────────
# DBS 3.0 多區版 — 環境變數範本
#
# 設定位置：Vercel → 你的 Project → Settings → Environment Variables
# 本檔案只作文件用途（.env.example），唔會被打包進前端 JS。
# 切勿把真實值 commit 上 Git。
# ─────────────────────────────────────────────────────────────

# ── 平台萬用密鑰（後備通道）────────────────────────────────
# 只加這一個就夠；各區的 STAFF_TOKEN / ADC_TOKEN 不需要放上來。
# 設定後，還要在各區 Google Sheet 選單執行一次：
#   🏕️ DBS 管理 → 🔐 設定平台萬用密鑰（只會儲存 SHA-256 雜湊）
# 說明見 PLATFORM_KEY.md
SUPER_KEY=

# ── 各區 API Key（每接入一區加一條）────────────────────────
# 由該區 setupSystem() 產生；前端只會經 /api/proxy 使用，不會外洩。
DBS_SKW_APIKEY=

# （新區範本）
# DBS_CHW_APIKEY=
